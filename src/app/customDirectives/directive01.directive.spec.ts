import { Directive01Directive } from './directive01.directive';

describe('Directive01Directive', () => {
  it('should create an instance', () => {
    const directive = new Directive01Directive();
    expect(directive).toBeTruthy();
  });
});
